{% import '_macros.twig' as macros %}
{% set isEdit = order.id is not empty %}
<div class="card">
    <div class="card-header">
        <span class="fas fa-{{ isEdit ? 'pencil' : 'plus' }}"></span>
        &nbsp;{{ isEdit ? 'Edit' : 'Add' }} order
        {{ macros.alert_label_inline() }}
    </div>
    <div class="card-body">
        {{ form_start(form) }}
            <div class="row mb-3">
                <div class="col-md-6">
                    <div class="input-group">
                        {{ form_label(form.createdAt) }}
                        {{ form_widget(form.createdAt) }}
                    </div>
                </div>
            </div>

            <div class="row mb-3">
                <div class="col-md-12">
                    <div class="input-group">
                        {{ form_label(form.customer) }}
                        {{ form_widget(form.customer) }}
                    </div>
                </div>
            </div>

            <div class="row mb-3">
                <div class="col-md-2">
                    <div class="input-group">
                        {{ form_label(form.quantity) }}
                        {{ form_widget(form.quantity) }}
                    </div>
                </div>
                <div class="col-md-10">
                    <div class="input-group">
                        {{ form_label(form.product) }}
                        {{ form_widget(form.product) }}
                    </div>
                </div>
            </div>

            <div class="row mb-3">
                <div class="col-md-3">
                    <div class="input-group">
                        {{ form_label(form.buyPrice) }}
                        {{ form_widget(form.buyPrice) }}
                    </div>
                </div>

                <div class="col-md-3">
                    <div class="input-group">
                        {{ form_label(form.price) }}
                        {{ form_widget(form.price) }}
                    </div>
                </div>

                <div class="col-md-3">
                    <div class="input-group">
                        {{ form_label(form.profit) }}
                        {{ form_widget(form.profit) }}
                    </div>
                </div>

                <div class="col-md-3">
                    <div class="input-group">
                        {{ form_label(form.amount) }}
                        {{ form_widget(form.amount) }}
                    </div>
                </div>
            </div>

            <div class="row mb-3">
                <div class="col-md-12">
                    <div class="input-group">
                        {{ form_label(form.note) }}
                        {{ form_widget(form.note) }}
                    </div>
                </div>
            </div>

            <div class="row mb-3">
                <div class="col-md-12">
                    <div class="input-group">
                        {{ form_label(form.url) }}
                        {{ form_widget(form.url) }}
                    </div>
                </div>
            </div>

            {{ form_widget(form.id) }}

            <div class="row mb-0">
                <div class="col-md-3">
                    <button class="btn btn-{{ isEdit ? 'primary' : 'success' }}" type="submit">
                        {{ isEdit ? 'Save' : 'Add' }}
                    </button>
                </div>
                <div class="col-md-3">
                    <div class="form-check">
                        {{ form_widget(form.paid) }}
                        {{ form_label(form.paid) }}
                    </div>
                </div>
                <div class="col-md-3">
                    <div class="form-check">
                        {{ form_widget(form.shipped) }}
                        {{ form_label(form.shipped) }}
                    </div>
                </div>
                <div class="col-md-3">
                    {% if isEdit %}
                        <button class="btn btn-danger float-end"
                                type="button"
                                v-ajax-delete
                                data-url="{{ path('orders.delete', {id: order.id}) }}"
                                data-redirect="{{ path('orders') }}"
                        >
                            <span class="fas fa-xmark"></span> Delete
                        </button>
                    {% endif %}
                </div>
            </div>
        {{ form_end(form) }}
    </div>
</div>
