{% extends "layout.twig" %}

{% block title %}
    {% if tag.id is not empty %}{{ tag.title }}{% else %}Add tag{% endif %}
{% endblock %}

{% block content %}
    <div class="row"
         id="entity-profile"
    >
        <div class="col-md-4 pr-5">
            {% include "tags/edit-form.twig" %}
        </div>
        <div class="col-md-5">
            {% include('tags/edit-panel.twig') %}
        </div>
    </div>
{% endblock %}
